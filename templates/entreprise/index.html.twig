{% extends 'base.html.twig' %}

{% block title %}Hello EntrepriseController!{% endblock %}

{% block contenu %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<table>
    <thead>
        <tr>
            <th>Raison Sociale</th>
            <th>SIRET</th>
            <th>ADRESSE</th>
            <th>CP</th>
            <th>VILLE</th>
            <th> </th>
            
            
        </tr>
    </thead>
    <tbody>
        {% for entreprise in entreprises %}
            <tr>
                <td>
                    <a href="{{ path('entreprise_show',{'id':entreprise.id}) }}">
                    {{ entreprise.raisonSociale }}</a>
                </td>
                <td> {{entreprise.adresse}} </td>
                <td> {{entreprise.siret }} </td>
                <td> {{ entreprise.cp }} </td>
                <td> {{ entreprise.ville}} </td>
                <td>
                <a href=" {{ path('entreprise_edit',{'id':entreprise.id}) }}">Modifier></a>
                
                
                

            </tr>
        {% endfor %}
    </tbody>
</table>
<a href=" {{ path('entreprise_add')}}">Ajouter</a>
{% endblock %}
